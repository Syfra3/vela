version: mcp-stock-chef-graph-correctness
implemented: true
status: scn-007-complete

workflow:
  spec: specs/hard_spec.md
  feature: features/mcp_stock_chef_graph_correctness.feature

scenarios:
  - SCN-001
  - SCN-002
  - SCN-003
  - SCN-004
  - SCN-005
  - SCN-006
  - SCN-007

completion_notes:
  - SCN-001 is implemented for the Vela MCP stock-chef graph correctness workflow.
  - SCN-002 is implemented for MCP graph source evidence in query responses.
  - SCN-003 is implemented for MCP freshness reason evidence when build or update guidance is recommended.
  - SCN-004 is implemented for canonical MCP tool names that do not duplicate the server name prefix.
  - SCN-005 is implemented for preferring the active real stock-chef workspace over same-name dep-eval corpus evidence in MCP graph-query responses.
  - SCN-006 is implemented for returning an explicit MCP ambiguity response when same-name stock-chef corpora exist without an active workspace or explicit graph selector, including symbol-only graph-backed queries that do not mention the duplicate project name.
  - SCN-007 is implemented for reporting a missing, invalid, or unreadable active stock-chef workspace graph without silently answering from an available dep-eval stock-chef corpus.
  - Later scenarios in `features/mcp_stock_chef_graph_correctness.feature` are intentionally outside this completion marker.
  - Approval evidence is recorded in `specs/.approved` for `features/mcp_stock_chef_graph_correctness.feature`.
  - No runtime behavior is introduced by this marker; it only records Bob review precondition evidence.

evidence:
  hard_spec: specs/hard_spec.md
  feature: features/mcp_stock_chef_graph_correctness.feature
  approval_marker: specs/.approved
  quality_gates: .uncle-bob/quality-gates.yaml
  tdd_log: .uncle-bob/tdd-log.md
  judge_report: reports/judge_report.md
